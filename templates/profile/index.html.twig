{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil de {{app.user.getFirstname()}} {{app.user.getLastname()}} </h1>
    {% for message in app.flashes('message') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
    <div class="p-3 admin white mt-4">
        <div><span class="bold">Nom d'utilisateur : </span>{{ app.user.getUsername() }}</div>
        <br/>
        <div><span class="bold">Nom : </span>{{ app.user.getLastname() }}</div>
        <br/>
        <div><span class="bold">Prénom : </span>{{ app.user.getFirstname() }}</div>
        <br/>
        <div><span class="bold">Email : </span>{{ app.user.getEmail() }}</div>
        <br/>
        <br/>
         <div>
            <p><a href="/profil/edit">Modifier le profil</a></p>
            <p><a href="/profil/edit_pass">Modifier le mot de passe</a></p>
         </div>
         
    </div>
{% endblock %}